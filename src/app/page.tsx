import Image from "next/image";

export default function Home() {
  return (
    <div className=" flex justify-center min-h-screen items-center font-[family-name:var(--font-geist-sans)]">
      <main>home</main>
    </div>
  );
}
